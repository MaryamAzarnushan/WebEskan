<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation_resident"
    app:startDestination="@+id/nav_home_resident"
    android:layout_height="match_parent"
    android:layout_width="match_parent">

    <fragment
        android:id="@+id/nav_home_resident"
        android:name="com.azarnush.webeskan.Resident_panelFragment"
        android:label="پنل ساکنین"
        tools:layout="@layout/fragment_resident_panel" />

    <fragment
        android:id="@+id/nav_home"
        android:name="com.azarnush.webeskan.ui.home.HomeFragment"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_home" />
    <fragment
        android:id="@+id/nav_exit_resident"
        android:name="com.azarnush.webeskan.ExitFragment"
        tools:layout="@layout/fragment_exit" />

    <fragment
        android:id="@+id/nav_exit_Account_resident"
        android:name="com.azarnush.webeskan.ExitFragment"
        tools:layout="@layout/fragment_exit" />


</navigation>